using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInvintory : MonoBehaviour
{
    private Dictionary<int, GameObject> AvailableBodies;
    private Dictionary<int, GameObject> AvailableCabins;
    private Dictionary<int, GameObject> AvailableThrust;

	// Use this for initialization
	//void Start ()
 //   {
 //       for (int i = 0; i <= ShipYard.shiprite.mainBody.Length; ++i)
 //       {
 //           AvailableBodies[i + 1] = ShipYard.shiprite.mainBody[i];
 //       }
 //       for (int i = 0; i <= ShipYard.shiprite.cntlUnit.Length; ++i)
 //       {
 //           AvailableCabins[i + 1] = ShipYard.shiprite.cntlUnit[i];
 //       }
 //       for (int i = 0; i <= ShipYard.shiprite.thruster.Length; ++i)
 //       {
 //           AvailableThrust[i + 1] = ShipYard.shiprite.thruster[1];
 //       }
	//}
	
	void Update () {
		
	}
}
